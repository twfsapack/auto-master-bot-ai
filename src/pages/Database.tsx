
import { ProblemsList } from '@/components/database/ProblemsList';
import Layout from '@/components/common/Layout';

const Database = () => {
  return (
    <Layout>
      <ProblemsList />
    </Layout>
  );
};

export default Database;
